{
  "name": "business-tasks-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "lint": "node --check src/rating.js && node --check src/store.js && node --check src/server.js && node --check public/app.js"
  }
}
